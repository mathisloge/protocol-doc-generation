\section{{^ frames.title ^}}

{% if existsIn(frames, "abstract") %}
    {^ frames.abstract ^}
{% endif %}

{% for fkey, frame in frames.frames %}
    \subsection{{^ frame.name ^}}
    {^ frame.description ^}
{% endfor %}
